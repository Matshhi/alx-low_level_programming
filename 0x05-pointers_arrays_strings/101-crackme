#include <stdio.h>
#include <string.h>

int main()
{
char password[10];
printf("Enter the password: ");
scanf("%s", password);
if (strcmp(password, "crackme123") == 0)
{
printf("Congratulations, you cracked the code!\n");
}
else
{
printf("Wrong password, try again.\n");
}
return (0);
}
